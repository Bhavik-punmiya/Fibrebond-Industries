// LoginPage.tsx
import React from 'react';
import LoginComponent from '../components/LoginComponent'; // Adjust the path as necessary

const LoginPage: React.FC = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default LoginPage;
